---
type: ledger
updated: '2026-06-17'
gates:
  - id: newnode
    name: 新建节点
    streak: 0
    threshold: 5
    state: gated
    note: 无人值守新建 wiki 节点，连续 5 次批准零驳回 → 自动批准
  - id: xedge
    name: 跨域边（built_on）
    streak: 0
    threshold: 5
    state: gated
    note: agent→web/data/infra 的跨域 built_on 边，连续 5 次 → 自动批准
  - id: t0-merge
    name: T0 合并
    streak: 0
    threshold: 5
    state: gated
    note: verified 数值/benchmark 合并进 data.db
  - id: retire
    name: 退役触发
    streak: 0
    threshold: 0
    state: locked
    note: 退役协议（封 valid_to + 插新行）永久人工
  - id: disputed
    name: 数值分歧
    streak: 0
    threshold: 0
    state: locked
    note: 数值/事实分歧永久人工
---

# 审批账本

信任账本：每类高危写入记 `streak`/`threshold`/`state` + append-only 审计日志。
规则：连续 threshold 次批准零驳回 → `state: auto`（无人值守可直写但仍记账）；**一次驳回 streak 清零**、已 auto 降级。
`retire` / `disputed` 永久人工（state: locked）。

## 审计日志（append-only）

- 2026-06-17 · 账本初始化（tech_air 建库）。
