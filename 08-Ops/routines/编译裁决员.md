---
type: routine
routine: ingest
name: 编译裁决员
kind: llm
status: active
sort: 5
trigger: 事件触发（投喂材料 /「编译这篇」/ 分诊员、研究员委托）
domains: [agent, web, data, infra]
reads: "源材料 · _ontology.md 契约（agent 母本）· canonical 名录(aliases)"
writes: "wiki 节点 + data.db（经闸门）"
budget: 逐项抽名词 · 六档时间裁决
escalation: newnode / retire / disputed / xedge（按审批账本状态分流）
last-run:
last-result:
---

# 编译裁决员 · ingest

抽取 → 实体/概念判据 → 六档时间裁决 → 受控词表校验 → 退役协议 → 落库。
**所有进 wiki 的知识必经此闸**（分诊员、研究员、答题员都没有绕过闸门的写权限）。
无人值守上下文中，gate 非 `auto` 的高危写入投递 `08-Ops/review/` 候选而非直写；跨域 `built_on` 边走 xedge gate。
