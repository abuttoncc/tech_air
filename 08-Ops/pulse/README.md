---
type: pulse-index
---

# 08-Ops/pulse · 今日启发束归档

[[心跳员]] 每日一束的落点。每期 `{date}.md`（`type: pulse`，frontmatter 带 `cards[]`），
Dashboard「今日 Pulse」区读最新一期渲染成启发卡。**append-only 归档：过期不删，次日生成新一束。**
卡片只指路、不进正典——要落知识走 `recall`/`ingest`/候选 gate。

> 还没有任何一期 → Dashboard 显示「心跳未起」。终端说「生成今日 pulse」即可生成。
