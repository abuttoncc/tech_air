# 07-QA · 问答库

一组站着不动的问题。动态问题（`mode: dynamic`）的答案是**时间序列**（append-only，每次站在 `as-of=今天` 重答、旧答案不删）。

由[[答题员]]/daily-routine 扫到期题逐个 `recall → 复盘 → 推演 → 缺口检测 → 追加答案`，结论回写 frontmatter `last-summary` 供 Dashboard 时间线读取。

> 示例题（待建）：「我对 RAG vs 长上下文 的当前判断」「自建 agent 编排 vs 用框架 的取舍」——这类立场会随学习漂移，适合做动态问题。
