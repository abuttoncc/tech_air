# Inbox · 收集箱

学习散文、踩坑记录、研究课题的收集箱，每条独立文件。这里**不要求本体严谨**——用研究笔记模板写「困惑 / 目标 / 思路 / 线索」，学透后用 `/auto-wiki ingest` 编译进 `wiki/`。

- 新档 frontmatter 带 `compiled: false`；分诊员/digest-inbox 按它识别待消化。
- 编译方向单向：`Inbox → ingest → wiki`，不反向。
